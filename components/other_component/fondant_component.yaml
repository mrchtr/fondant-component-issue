name: Other component
description: Other component
image: other_component:latest

consumes:
    sequence:
        type: string
    sequence_checksum:
        type: string

produces:
    sequence:
        type: string
    sequence_length:
        type: int64
    sequence_checksum:
        type: string

